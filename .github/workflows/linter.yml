name: Lint

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: "install linter"
        run: sudo apt-get install -y pylint
      - name: "run linter"
        run: pwd && ls ..
